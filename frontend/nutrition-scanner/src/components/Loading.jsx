const LoadingView = () => (
  <div>
    <h2>Analyzing Nutrition...</h2>
    <p>This may take a moment as we cross-reference ingredients against your chosen filters.</p>
    <p>Current Status:</p>
  </div>
);

export default LoadingView;
